# PostgreSQL Database URL
# Format: postgresql://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL="postgresql://isp_user:%23Dotmac246@localhost:5432/isp-project"

# A strong, unique secret key for encoding JWT tokens.
# You can generate one using: openssl rand -hex 32
SECRET_KEY="your-super-secret-key-that-is-at-least-32-characters-long"

# CORS Origins (comma-separated list of allowed frontend URLs)
CORS_ORIGINS="http://localhost:5173,http://127.0.0.1:5173"

# Celery Configuration (using Redis)
CELERY_BROKER_URL="redis://localhost:6379/0"
CELERY_RESULT_BACKEND="redis://localhost:6379/0"

# Test Database URL (use a separate database for tests)
TEST_DATABASE_URL="postgresql://isp_user:%23Dotmac246@localhost:5432/isp-project-test"